# Sonar Project Configuration
sonar.projectKey=prophezeiung
sonar.projectName=Prophezeiung
sonar.projectVersion=0.1.0

# Source encoding
sonar.sourceEncoding=UTF-8

# Sources
sonar.sources=src
sonar.tests=src

# Test file patterns (tests live alongside source files)
sonar.test.inclusions=**/*.test.ts,**/*.test.tsx

# Exclusions from analysis
sonar.exclusions=\
  node_modules/**,\
  .next/**,\
  .scannerwork/**,\
  public/**,\
  prisma/migrations/**,\
  docs/**,\
  scripts/**,\
  **/*.test.ts,\
  **/*.test.tsx

# Coverage report (if using vitest/jest with lcov)
# Main tests (jsdom) + node tests (session)
sonar.javascript.lcov.reportPaths=coverage/lcov.info,coverage-node/lcov.info

# Coverage exclusions (server-side code not testable in jsdom)
sonar.coverage.exclusions=\
  src/**/*.test.ts,\
  src/**/*.test.tsx,\
  src/**/*.d.ts,\
  src/**/index.ts,\
  src/app/layout.tsx,\
  src/app/**/page.tsx,\
  src/app/**/layout.tsx,\
  src/lib/auth/**/*.ts,\
  src/lib/db/**/*.ts,\
  src/lib/sse/**/*.ts,\
  src/proxy.ts,\
  src/instrumentation.ts

# TypeScript configuration
sonar.typescript.tsconfigPath=tsconfig.json
